---

import githubImg from '@/img/github-dark.png'
export interface Props {
  filename: string;
}

const { filename } = Astro.props as Props;

---
{
    (filename == "github") &&
    <img src={githubImg.src} height="32px"/>
}
